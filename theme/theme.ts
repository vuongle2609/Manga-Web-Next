import { createTheme } from '@nextui-org/react';

export const theme = createTheme({
    type: "light",
    theme: {
        fonts: {
            sans: "Quicksand, sans"
        },
        colors: {
            // brand colors
            primary: '#fca815',
            grayColor: '#8a8a8a'
        },
    }

});
